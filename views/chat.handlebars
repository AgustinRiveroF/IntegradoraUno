  <div id="informacion">
        <h2> CHAT CODERHOUSE </h2>
        <h5> Conectados </h5>
        <ul>
            {{#if user}}
            <li><p> {{user.first_name}} {{user.last_name}} </p></li>
            {{else}}
            <p>No user</p>
            {{/if}}
        </ul>
    </div>

    <div id="contenidoPrincipal"></div>

<div id="contenidoPrincipal"></div>

<div id="chat"></div>

<form id="chatForm">
    <input type="text" name="message" id="message" placeholder="Envia un mensaje aquí">
    <input type="submit" value=" > " id="botonEnviar">
</form>

<style>
        body{
            background-color: rgba(52,53,65);
            display: flex;
            height: 100vh;
            margin: 0;
        }
        #informacion {
            flex: 0;
            padding: 30px;
            border-right: 1px solid #ccc;
        }
        #contenidoPrincipal {
            flex: 2; 
            padding: 20px;
        }

        h2{
            text-align: center;
            color: azure;
        }
        h3 {
            color: brown;
            text-align: center;
        }
        .vista {
            border: 2px solid black;
            border-radius: 5px;
            text-align: center;
            margin-left: 40rem;
            margin-right: 40rem;
            margin-top: 5rem;
            background-color: aqua;
        }
        form {
            margin-bottom: 5rem;
            text-align: center;
            position: fixed;
            bottom: -5%;
            width: 47%;
            padding: 20px;
            border: 1px solid #ccc;
            margin-left: 36%;
        }          
        #message {
            width: 50%;
            height: 1.5rem;
            text-align: center;
        }
        #botonEnviar{
            height: 1.8rem;
            font-weight: 800;
        }
        #chat {
            max-height: 35rem;
            overflow: auto;
            color: white;
            margin-left: 30%;
            position: fixed;
            width: 60%;
            top: 12%;
            font-weight: 500;
        }
        .toastify{
            position: fixed;
            margin-left: 25rem;
            width: 70%;
            height: 3rem;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: #3498db; 
            color: #fff; 
            z-index: 1000;
        }
        h5{
            color: white;
            font-size: medium;
        }
        p{
            color: rgba(245, 245, 3, 0.89);
        }
        li{
            color: white;
        }

    </style>

<script type="text/javascript" src="https://cdn.jsdelivr.net/npm/toastify-js"></script>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script src="/socket.io/socket.io.js"></script>
<script src="/js/chat.js"></script>
